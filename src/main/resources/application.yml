spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/bookshelf
    username: postgres
    password: Miro$hnik2005
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE

server:
  port: 8080